Feature: Elements page feature

    this is test add/remove elements functionality.

    @elements
    Scenario: verify elements can be added and removed
        Given home page should be loaded
        When i click on add remove link
        Then add few elements and remove those elements